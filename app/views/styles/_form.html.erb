<%= form_with(model: style, local: true) do |form| %>
  <% if style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(style.errors.count, "error") %> prohibited this style from being saved:</h2>

      <ul>
      <% style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
  <div class="form-group row">  
    <label for="name" class="col-2 col-form-label"><%= form.label :name %></label>
  <div class="col-sm-10">
    <input type="text" class="form-control" id= <%= form.text_field :name %>   
  </div>
  </div>
  </div>

    <div class="field">
  <div class="form-group row">  
    <label for="description" class="col-2 col-form-label"><%= form.label :description %></label>
  <div class="col-sm-10">
  <textarea class ="form-control" rows="3" id= <%= form.text_area :description %></textarea>  
  </div>
  </div>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-primary"   %>
  </div>
<% end %>
