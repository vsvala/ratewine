<h2>Create new rating</h2>

<%= form_for(@rating) do |f| %>
  <%= f.select :wine_id, options_from_collection_for_select(@wines, :id, :to_s)%> 
 
 score: <%= f.number_field :score %>

  <%= f.submit %>
<% end %>